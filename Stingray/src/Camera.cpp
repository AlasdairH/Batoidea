#include "Camera.h"

namespace Batoidea
{
	Camera::Camera(const int _viewportResolutionWidth, const int _viewportResolutionHeight)
	{

	}

	Ray Camera::getRay(const int _x, const int _y)
	{
		return Ray();
	}
}